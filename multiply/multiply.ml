(*This code does not execute properly. Try to figure out why. *)
(*I needed to put let in front of multiply. I added next two lines just as an example *)

let multiply x y = x * y;;
let result = multiply 2 5;;
print_int result;;
